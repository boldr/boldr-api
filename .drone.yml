debug: true
pipeline:
  build:
    image: node:7
    commands:
      - npm install
      - npm run test:ci
services:
  database:
    image: strues/boldr-pg
  redis:
    image: redis:3.2.6-alpine

  # publish:
  #   docker:
  #     registry: registry.boldr.io
  #     username: strues
  #     password: $$PASSWORD
  #     email: steven@strues.io
  #     repo: boldr/cms
  #     tag: $$BRANCH
  #     file: Dockerfile
  #     insecure: false
